spring:
  application:
    name: telangana-ball-badminton-association
  
  # Heroku PostgreSQL Database Configuration - Connection Test Only
  datasource:
    url: jdbc:postgresql://c1erdbv5s7bd6i.cluster-czz5s0kz4scl.eu-west-1.rds.amazonaws.com:5432/df72l64ipgnnt9
    username: uarq7169567h9a
    password: p2a3d8927b39fd36827c371f06b66377c30f5c1c121f030bb9a1ec6f30990201b
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 10000
      validation-timeout: 5000

  # JPA Configuration - NO SCHEMA CHANGES
  jpa:
    hibernate:
      ddl-auto: validate  # Only validate, don't create/update schema
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  # Disable Redis for testing
  data:
    redis:
      repositories:
        enabled: false

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api/v1

# Logging Configuration
logging:
  level:
    com.telangana.ballbadminton: INFO
    org.springframework.security: WARN
    org.hibernate: WARN
    com.zaxxer.hikari: INFO
  pattern:
    console: "%d{HH:mm:ss} %-5level %logger{36} - %msg%n"

# Application Configuration
app:
  jwt:
    secret: testSecretKey123456789012345678901234567890
    expiration: 86400000
  
  cors:
    allowed-origins: http://localhost:3000
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"
    allow-credentials: true

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always